/// Photos service — placeholder for V2 (ambient slideshow).
/// Will integrate with Google Photos API in a future release.
class PhotosService {
  const PhotosService();

  /// Returns a list of photo URLs (placeholder).
  List<String> getPhotos() => [];
}
